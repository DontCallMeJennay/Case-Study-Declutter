extends layout

block success
	div.indent
		h2 Success Stories
		div#story-list
			template(v-for='story in stories')
				story-item.row(:author='story.author' :title='story.title' :info='story.info' :date='story.date' :pic='story.pic')